{
  "name": "client",
  "version": "0.1.0",
  "private": true,
  "scripts": {

    "build": "next build",
    "start": "next start",
    "lint": "next lint",
    
    "dev": "concurrently \"pipenv run next dev\" \"pipenv run flask --app api/index run -p 8000 --reload\"",
    "flask-dev": "FLASK_DEBUG=1 pip3 install -r requirements.txt && python3 -m flask --app api/index run -p 8000 --reload",
    "next-dev": "next dev"


  },
  "dependencies": {
    "@heroicons/react": "^2.0.18",
    "axios": "^1.6.2",
    "next": "14.0.3",
    "react": "^18",
    "react-dom": "^18",
    "sass": "^1.69.5",
    "scss": "^0.2.4"
  },
  "devDependencies": {
    "eslint": "^8",
    "eslint-config-next": "14.0.3"
  }
}
